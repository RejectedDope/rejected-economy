// Global test setup — runs before every test file.
// Keep this minimal: no DOM setup needed for pure logic tests.

// Vitest globals (describe, it, expect) are injected via vitest.config.ts globals: true
